{
  "name": "liveroom-data-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for LiveRoomData uploads to Neon",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
